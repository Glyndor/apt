name: DCO

# Requires a Signed-off-by trailer on every commit in the pull request, via the
# shared reusable — the same gate every product repo runs.

on:
  pull_request:

permissions:
  contents: read

jobs:
  dco:
    uses: Glyndor/.github/.github/workflows/dco.yml@d2e4c3db2ae66befaae8399ba4cdf0074511c2c2 # v1.13.2
